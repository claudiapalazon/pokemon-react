import React from "react";
import "../stylesheets/App.scss";

class App extends React.Component {
  render() {
    return <div className="App">Hola mundo</div>;
  }
}

export default App;

// const items = this.props.clubs.map((club, index) => {return <Club name={club.name}});
